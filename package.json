{
    "name": "vedsaas-frontend",
    "version": "1.0.0",
    "description": "VedSAAS Frontend",
    "scripts": {
        "build": "echo 'No build step required, serving static files'",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "@capacitor/android": "^5.0.0",
        "@capacitor/core": "^5.0.0"
    },
    "devDependencies": {
        "@capacitor/cli": "^5.0.0"
    }
}